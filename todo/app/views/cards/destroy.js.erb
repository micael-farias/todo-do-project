// app/views/cards/destroy.js.erb

// Seleciona o card pelo data-card-id e aplica a animação de fade out, então remove do DOM
$('.card[data-card-id="<%= @card.id %>"]').fadeOut(300, function() {
  $(this).remove();
});
