<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Todo" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/Sortable/1.14.0/Sortable.min.js" integrity="sha512-w1mqU4f7Igzpduq9hJ01CpIKnWpl9kcLwRUXLBfYHACyugVp5isO4r1mTrYHLU1kC2tFk5mFEYHmpuQuKZyaHw==" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">

    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jqueryui/1.12.1/jquery-ui.min.js"></script>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Bootstrap JS (necessário para modals) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    <!-- Scripts necessários -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <!-- Inclua o jQuery UI para o sortable -->
    <script src="https://code.jquery.com/ui/1.13.1/jquery-ui.min.js"></script>

    <!-- Opcional: Inclua ícones do Bootstrap se ainda não estiverem incluídos -->



    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= javascript_include_tag "//www.google.com/jsapi", "chartkick" %>
  </head>

<body style="background-color: #1D2125">
    <!-- Navbar -->
 <nav class="navbar navbar-expand-lg bg-orange">
  <div class="container-fluid">
    <!-- Link para a página inicial -->
    <a class="navbar-brand text-white" href="<%= root_path %>">TO DO</a>
    <!-- Botão de colapso para dispositivos móveis -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" 
            aria-controls="navbarNav" aria-expanded="false" aria-label="Alternar navegação">
      <span class="navbar-toggler-icon"></span>
    </button>
    <!-- Conteúdo do menu de navegação -->
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto">
        <!-- Adicione links de navegação adicionais aqui, se necessário -->
      </ul>
      <!-- Seção de saudação e imagem do usuário com dropdown -->
      <%if current_user != nil%>
      <div class="d-flex align-items-center">
        <span class="text-white me-2">Vamos trabalhar Michael! <%= current_user.name %></span>

        <!-- Dropdown para o avatar -->
        <div class="dropdown">
          <a href="#" class="d-flex align-items-center" id="userDropdown" data-bs-toggle="dropdown" aria-expanded="false">
            <% if current_user.avatar.attached? %>
              <%= image_tag current_user.avatar.variant(resize_to_limit: [40, 40]), class: 'rounded-circle' %>
            <% else %>
              <%= image_tag 'default_avatar.png', class: 'rounded-circle', alt: 'Usuário', width: '40', height: '40' %>
            <% end %>

          </a>
          <ul class="dropdown-menu dropdown-menu-end custom-dropdown-menu" aria-labelledby="userDropdown">
            <li>  
            <a class="dropdown-item" href="<%= edit_user_registration_path %>" style="color:white">
              <i class="bi bi-gear me-2"></i> Configurações
            </a>
            </li>
            <li>
              <a class="dropdown-item" href="#" style="color:white">
                <i class="bi bi-person me-2"></i> Perfil
              </a>
            </li>
            <li><hr class="dropdown-divider"></li>
            <li>
              <%= link_to destroy_user_session_path, method: :delete, class: 'dropdown-item', style: 'color:white' do %>
                <i class="bi bi-box-arrow-right me-2"></i> Sair
              <% end %>
            </li>
          </ul>

        </div>
      </div>
      <%end%>
    </div>
  </div>
</nav>

    <!-- Container principal -->
    <div class="container-fluid" style="padding: 0px">
      <%= yield %>
    </div>
  </body>
</html>
